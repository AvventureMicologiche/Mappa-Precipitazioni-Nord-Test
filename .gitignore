# Local Netlify folder
.netlify

# Messaggio di allarme costruito al volo da check-fonti.js (non va nel repo)
alert-mail.eml

# Impostazioni locali di Claude Code: stanno nella cartella di lavoro di chi
# sviluppa, non nel repo (aggiunta il 28/8/2026, prima era solo non tracciata:
# bastava un `git add -A` distratto per portarsela dentro)
.claude/
